# Prompt for the company integration agent

I want to adapt our existing **Python LiveKit voice assistant** to evaluate the anticipatory-retrieval design in a cloned research repository. Keep the current application as a working baseline. Implement incrementally in the company repository; do not push company code, transcripts or knowledge into the research repository or external services without authorization.

First locate the cloned research repository. Read `40-Exercises/Sandbox/LiveKitAnticipationPoC/v4/COMPANY-AGENT-HANDOFF.md`, (or `v4/COMPANY-AGENT-HANDOFF.md` in the scoped export), then its `README.md`, `INTERFACE-AND-SCHEDULING.md`, source and relevant evidence. Read the company's applicable AGENTS instructions, dependency lockfile and actual STT/LLM/tools/TTS integration before editing. Report exact paths and versions you found. Do not assume that our SDK is LiveKit 1.8.0 or upgrade it just to match the PoC.

The intended optimization observes the **existing single STT stream**, builds executable queries from cumulative partial text, starts read-only speculative retrieval and checks candidate applicability against the committed final turn. Wait/no-query is allowed. A missed reuse is much less costly than accepting unsuitable evidence. Keep LLM, exact and actual embedding/cosine semantic Checkers as independent comparison configurations; do not silently introduce a hybrid or use hash similarity. Preserve the experimental defaults unless our evaluation justifies and we explicitly record a change.

Start with the bounded stages in the handoff:

1. Map the real provider's partial/final segment semantics, EOT hook, committed-history source, interruption lifecycle, answer/tool path and native preemptive setting. Preserve the current baseline and measure it. Identify unresolved facts through code and focused local checks first.
2. Define the company's retrieval contract. Replace the sample schema, Query validation, Builder descriptions, retrieval adapter, Checker representation and answer evidence format together. Northwind and its full-row query semantics are not our business knowledge. Do not allow silent loss of unsupported constraints or generated SQL. Specify final no-query handling explicitly; the PoC's Answerer can still rewrite after a wait and has a known wrong-entity failure.
3. Implement a feature-flagged adapter, initially off, with controlled lifecycle tests and then shadow mode. A shadow decision must not affect user-facing answers. Preserve existing tools, streaming, TTS and the ordinary retrieval route. Do not substitute the PoC's custom `PipelineLLM` wholesale: its `RAG_BUNDLE` marker only works with its private in-memory consumer and does not implement our tools.
4. Use authorized offline transcripts and independent labels for short independent questions, long questions and contextual follow-ups, including adversarial entity/constraint changes. Freeze versions and retain failures. Evaluate Builder fidelity, Checker false acceptance, answer correctness, latency and cost separately. Agree on acceptance limits before enabling evidence consumption.
5. Enable consumption only in a controlled test configuration that meets those limits, with a proven rollback flag. Measure actual STT/TTS voice latency and interruption behavior. Do not claim voice acceleration from scripted STT timing or simply from a successful reuse.

Keep native preemptive generation disabled for the first new integration arm unless we separately implement and validate its bundle/context lifecycle. If our original baseline uses it, preserve that baseline and make the comparison's configuration difference explicit. Maintain turn/context ownership; reject late/cancelled/cross-turn results; never wait for every speculative task; do not cancel a retrieval still shared by another candidate.

Use our approved company model and retrieval endpoints. Do not reuse personal DeepSeek credentials, copy `.env` files, or automatically run hundreds of model calls. For research-only reproduction, install **both** parent `requirements.txt` and `v4/requirements-v4.txt` in an isolated environment; semantic dependencies are separate. Keep company dependency management intact. No Jetson experiment is needed.

Deliver a concise integration map, reviewed domain contract, default-off implementation, test evidence and limitations, then a runbook showing baseline/shadow/controlled-enable/rollback commands for this actual project. Continue with authorized reversible work instead of repeatedly asking for permission. Ask only for missing company-specific decisions that cannot be established from the repository or existing authorization, and do not present unmeasured quality or latency benefits as achieved.
