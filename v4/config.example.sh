# Copy these settings into your shell; do not commit actual credentials.
# Base URL must include the compatible API prefix, e.g. /v1.
# No model, endpoint, API key or live call is configured by this example.
# export RAG_LLM_BASE_URL='https://YOUR-ENDPOINT/v1'
# export RAG_LLM_MODEL='YOUR-MODEL'
# export RAG_LLM_API_KEY_ENV='MY_EXISTING_KEY_VARIABLE'
# The named variable must already hold the API key; do not paste keys in chat.

export RAG_DB_HOST='127.0.0.1'
export RAG_DB_PORT='55432'
export RAG_DB_NAME='rag_fixture'
export RAG_DB_USER='rag_reader'
